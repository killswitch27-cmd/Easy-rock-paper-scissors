[project]
name = "rock-paper-scissors"
version = "0.1.0"
description = "Simple rock-paper-scissors game in python, made with uv"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ipykernel>=7.2.0",
]
